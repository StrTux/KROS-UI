// Sonner (Toast notifications) — coming soon
import React from 'react';
import { View, Text } from 'react-native';

export const Sonner = () => (
  <View>
    <Text>Sonner coming soon</Text>
  </View>
);

export default Sonner;
